apiVersion: 1

providers:
  - name: TraceRuntime
    orgId: 1
    folder: TraceRuntime
    folderUid: traceruntime
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards
